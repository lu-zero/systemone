[package]
name = "systemone-facet"
version.workspace = true
edition.workspace = true
license.workspace = true
# facet needs a newer toolchain than the workspace floor, which tracks maki's MSRV.
rust-version = "1.90"
repository.workspace = true
readme = "README.md"
description = "Typed systemone choice criteria via facet reflection"

[dependencies]
systemone = { workspace = true }
facet = "0.46"

[dev-dependencies]
serde = { workspace = true }
futures-lite = { workspace = true }
