//! Stub.
//!
//! Explores [`facet`](https://github.com/facet-rs/facet) reflection as an alternative to
//! `systemone-macro`'s derive-macro approach for typed `choice`/`score` criteria: introspect
//! a plain enum's variants at runtime instead of generating code for them at compile time.
//! Not implemented yet; nothing here is wired up until one approach is chosen.
